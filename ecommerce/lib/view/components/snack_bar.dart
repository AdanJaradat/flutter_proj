import 'package:flutter/material.dart';

class SnackBarComp extends StatelessWidget {
  const SnackBarComp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
